- labelling_form_for :account_password, @user, :url => account_password_path, :html => { :id => 'set_password_form' } do |f|
  = f.error_messages
  
  = f.password_field :password
  = f.password_field :password_confirmation
  
  = f.submit_update

%script ui.initSetPassword();