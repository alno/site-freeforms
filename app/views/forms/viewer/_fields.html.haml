- form.fields.each_with_index do |field,i|
  %p
    %b= field.title + ':'
    =t 'fields.' + field.type.to_s
    %ul
      - if field.default
        %li
          %b По умолчанию:
          = field.default
            
      - if field.required?
        %li
          %b Обязательно к заполнению